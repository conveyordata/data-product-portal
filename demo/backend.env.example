# Portal Backend Webhook Configuration
# Add these to your backend/.env or export them before running the backend

# Webhook URL pointing to the provisioner
WEBHOOK_URL=http://localhost:8090

# Optional webhook secret for signing requests
WEBHOOK_SECRET=demo-secret-key

# Other backend settings remain the same
