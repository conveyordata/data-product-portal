[mypy]
# sdk/api_client/ is auto-generated from the OpenAPI spec and uses a T TypeVar
# reuse pattern that mypy cannot follow. Errors are suppressed here rather than
# in each package that imports from it.
[mypy-sdk.api_client.*]
ignore_errors = True
