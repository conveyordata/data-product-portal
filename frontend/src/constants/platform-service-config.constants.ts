import type { PlatformServiceConfigMapping } from '@/types/platform-service-config';

export const PLATFORM_SERVICE_CONFIG_MAPPING: PlatformServiceConfigMapping = {
    'aws-s3': { identifiers: [] },
    'aws-glue': { identifiers: [] },
};
